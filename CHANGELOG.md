# Changelog

## Unreleased

初版。规则、核心、两端客户端、难度档位全部经过实测。

### 规则

- 每种颜色恰好一个雷；每行/列显示该行/列雷的总数
- 颜色全程可见，隐藏的是"哪个格子是雷"
- 右键循环标记（未标记 → `?` → 旗），**每种颜色最多一面旗**
- 左键翻开是**可选的风险操作**：不翻开也能靠纯推理通关
- 胜利 = 每种颜色的旗都插在真正的雷上

### 核心

- 唯一解保证（精确求解器 + 行列容量/强制剪枝）
- 纯逻辑可推保证（单例 / 行满 / 行列恰好三条规则）
- 生成器：采样 → 验证 → 拒绝，单次尝试零点几毫秒
- 会话层：状态机、每色一旗、防手滑（已插旗不可翻开）
- 提示：以玩家当前进度为种子跑求解器，取下一步；旗子标错会提示矛盾
- 复盘：完整推导链，可跳到任一步看当时已知的信息
- 最佳成绩：按配置记录，排序为 提示数 → 用时 → 翻开次数

### 客户端

- Web（Vite + vanilla TS，无框架）：产物 ~45 kB JS (gzip 15 kB) + worker + CSS
- 桌面（Tauri 2）：exe 3.0 MB，NSIS 安装包 1.1 MB，Rust 侧仅 11 行
- 中英双语、键盘操作（方向键 / F / Ctrl+N）、aria 标签随语言切换
- 主页卡片式导航（开始游戏 / 游戏设置 / 历史 / 导入导出）；对局中隐藏设置
- 复盘时棋盘居左、推理链居右，同屏不翻页
- 数据位置可用系统文件夹选择器更改并即时生效
- PWA 离线（manifest + 缓存优先 Service Worker）
- 生成跑在 Web Worker，不阻塞主线程

### 工程与规范

- 数据文件夹结构 `data/{settings,history,maps,logs,backups}`；安全写入（临时 → 校验 → 替换）
- 损坏设置 → 默认值 + `.bad`；损坏历史 → 尝试 `.bak`（**逐条字段校验**后才采用）
- 安装位置迁移：复制 → 验证 → 清理源；失败则源数据完整保留
- 桌面端文件 I/O 改用 **Rust 原生命令**（`tauri-plugin-fs` 在本环境拒绝所有路径，已弃用）
- 桌面端不注册 Service Worker（曾导致更新后仍加载旧页面）
- 日志落盘到 `data/logs/app.log`，滚动保留 200 行
- 备份除下载外也存入 `data/backups/`，滚动保留最近 10 份
- 调试模式暴露 §29 全部项：种子、区域 ID、颜色 ID、求解器状态、生成评分、失败诊断

### 工程

- 核心测试 29 项（含"求解器推出的雷位必须等于真值"这条关键断言）
- `npm run mass` 量产统计工具：每档成功率、耗时分位、推理步数
- 难度档位由实测数据决定，并附在 `src/core/tiers.ts`
- AGPL-3.0 许可与第三方声明

### 已知限制

- 大棋盘 + 高雷数（如 14×14/12、16×16/12+）仍无法生成唯一解棋盘。
  目前靠 `estimateFeasibility()` 预警 + 推荐组合兜底；要支持需改用"修复式"
  生成（定位导致多解的矩形交换并重着色一个格子破坏它）。
- 难度"手感"尚缺真人验证——推理步数只是代理指标。
- 桌面端依赖系统 WebView2（Windows 11 自带，Windows 10 需引导安装）。
