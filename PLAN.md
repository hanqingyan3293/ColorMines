# Color Mines 执行列表

原始需求（唯一不可推翻的）：**规则简单、有难度档位、类似扫雷的小游戏、桌面端 + web 端。**

---

## 已锁定的决定

| 项 | 决定 |
|---|---|
| 规则 | 每格一色，**每色恰好一个雷**；每行/列显示该行列雷总数 |
| 信息划分 | **颜色全程可见**，隐藏的是"哪个格是雷" |
| 保证 | 唯一解 **+** 纯逻辑可推 |
| 交互 | 插旗 = 标记雷；翻开 = 可选的风险操作（不翻也能赢）；胜利 = 标记对 K 个雷 |
| 技术栈 | TypeScript 核心 + **Tauri 桌面端** + 同一份核心的 Web/PWA |
| 棋盘 | 按需生成；参数完全自由；允许"生成中"提示 |

---

## 阶段 0 — 可行性（已完成 ✅）

- [x] 0.1 规则与信息划分定稿
- [x] 0.2 核心：棋盘模型、精确求解器、逻辑求解器、生成器
- [x] 0.3 CLI 量产工具（`npm run mass`）
- [x] 0.4 档位实测与定档（6 档全部 100% 成功，推理步数单调递增）

**这一步换来的关键认知**

- 「逻辑停滞」在所有配置中一次都没出现 → 唯一性一旦成立，可解性自动满足。
- 唯一性的敌人是**矩形交换退化**；增加雷数反而更糟。
- 可行域由 **雷数 × 格数** 共同决定，经验阈值 ≈ 2000。

---

## 阶段 1 — 能玩的最小闭环（当前）

- [x] 1.1 **会话层**：翻开、插旗、胜负判定、计时、点击计数、成绩口径
- [x] 1.2 **Web UI**：棋盘渲染（颜色 + 行列数字）、点击/右键交互、HUD
- [x] 1.3 **生成接入**（Web Worker，不阻塞主线程）：异步生成、生成中指示、失败提示 + 推荐可用组合
- [x] 1.4 **核心单测**（29 项，含「求解器推出的答案 == 真值」）：不变式、两个求解器的正确性、随机对局回归
- [x] 1.5 **难度选择**（六档预设 + 自定义可行性预警）：六档预设 + 自由参数入口（带可行性预警）

验收：浏览器里从"选难度"到"插旗获胜"完整走通，且生成的每一局都唯一可推。

---

## 阶段 2 — Tauri 桌面端

- [x] 2.1 Tauri 骨架，**复用阶段 1 的同一份核心与 UI 代码**（Rust 侧仅 11 行）
- [ ] 2.2 桌面专属：菜单栏、数据目录（已做：独立窗口 1200×880、Ctrl/⌘+N 新对局）
- [x] 2.3 构建安装包并实测（安装 → 运行 → 卸载；exe 3.0 MB / 安装包 1.1 MB）

验收：桌面端与 web 端行为一致（同一核心、同一份 UI）。

---

## 阶段 3 — 打磨

- [x] 3.1 提示（用玩家当前进度作种子跑逻辑求解器，取下一步；旗子标错会提示矛盾）
- [x] 3.2 复盘（完整推导链，可跳到任一步看当时已知信息）
- [x] 3.3 最佳成绩（按配置记；排序：提示数 → 用时 → 翻开次数，用提示的排在无提示之后）
- [x] 3.4 **i18n**（~65 条，含求解器步骤；求解器存 `noteKey` 而非中文文本）
- [x] 3.5 键盘（方向键移动 / 空格翻开 / F 标记 / Ctrl+N）与无障碍（aria 随语言切换）
- [x] 3.6 PWA 离线（manifest + 缓存优先 Service Worker）

---

## 阶段 4 — 发布（全部完成 ✅）

- [x] 4.1 CI（core / web / ui / desktop 四个 job，见 .github/workflows/ci.yml）
- [x] 4.2 许可（LICENSE = AGPL-3.0）+ THIRD-PARTY-NOTICES.md
- [x] 4.3 安装包 1.1 MB + 便携版 zip 1.3 MB（`scripts/package-portable.py`）
- [x] 4.4 README（含实测档位表与结构说明）+ CHANGELOG

---

## 明确不做（上一版的教训）

1. **不做两套实现** —— 核心只有一份，两端共用（上一版这个坑最深）。
2. **不把参数旋钮当卖点** —— 默认只给难度档位，自由参数放高级设置。
3. **不在可行性验证前写 UI** —— 阶段 0 已经挡掉了一次返工。
4. **不做静默 fallback** —— 生成失败、WASM/核心加载失败都要可见。
5. **不预生成棋盘包** —— 按需生成已实测够快（最差 p95 118ms）。

---

## 风险与未决项

| 项 | 状态 |
|---|---|
| 大棋盘 + 高雷数（14×14/12、16×16/12+）仍不可行 | 用 `estimateFeasibility()` 预警 + 推荐组合；真要支持需换"修复式"生成算法 |
| Tauri 首次构建耗时 | cargo 本机已有；首次编译可能较慢 |
| 难度"手感"尚无真人验证 | 推理步数只是代理指标，阶段 1 结束后需要试玩 |
| 成绩口径（用时 vs 点击数 vs 是否翻开） | 待定，建议阶段 1 先都记下来 |
